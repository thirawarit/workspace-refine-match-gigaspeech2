# Runtime dependencies for the Thai ASR batch-inference pipeline.
#
# NOTE: NVIDIA NeMo is deliberately ABSENT from this file.
# The model requires `EncDecRNNTBPEModelWithPrompt`, which stock `nemo_toolkit`
# does not contain. NeMo must be built from source at pinned commit 907edfd:
#
#     git clone https://github.com/NVIDIA/NeMo && cd NeMo
#     git checkout 907edfd && pip install -e .
#
# `setup_and_run.sh` does this and verifies the commit. Do not "fix" this by
# adding a pip pin here — there isn't one that works.

torch==2.5.1
huggingface-hub==0.26.2
PyYAML==6.0.2
soundfile==0.12.1
numpy==1.26.4
tqdm==4.67.1

# Testing
pytest==8.3.4
pytest-mock==3.14.0
